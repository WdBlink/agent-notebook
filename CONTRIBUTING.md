# 协作开发与分支约定

本仓库使用 `功能分支 → dev → develop → main → 版本标签` 的流程。`dev` 和 `develop` 在本项目中有不同职责，不能混用。

## 分支用途

| 分支 | 用途 | 来源与合并方向 |
| --- | --- | --- |
| `main` | 稳定发布基线；正式版本标签只在通过验收的 `main` 提交上创建 | 接收 `develop` 的发布 PR，以及紧急修复 PR |
| `dev` | 日常开发集成；合入已完成、通过相关检查的功能，保持可运行 | 功能分支从这里创建，完成后通过 PR 合回；准备发布时向 `develop` 提交 PR |
| `develop` | 发布前联调、验收和稳定化；冻结本次发布范围 | 接收选定时点的 `dev`；验收期间只接收本次发布所需修复、版本号和发布说明；通过后向 `main` 提交发布 PR |
| `codex/<topic>`、`feat/<topic>`、`fix/<topic>` | 单个功能或常规修复的短期工作分支；Codex 默认使用 `codex/` | 从最新 `dev` 创建，合回 `dev` 后删除 |
| `hotfix/<topic>` | 已发布版本的紧急修复 | 从 `main` 创建，PR 合回 `main`，随后同步到 `develop` 和 `dev` |

三条长期分支不得删除、强推或重写共享历史。暂不新增长期 `release` 分支，发布稳定化由 `develop` 承担。现有历史功能和 `release/*` 分支保留，核对差异后再决定如何合并或清理，不自动当作当前开发基线。

## 日常开发

1. 更新 `dev`，再创建一个任务对应的功能分支。无依赖的任务不要从他人未合并的功能分支继续开发。
2. 提交 PR 时明确选择 `dev` 为目标分支，描述用户影响和实际运行的检查；大功能可提前开草稿 PR。
3. 由另一位开发者审阅。解决审阅意见，并完成 [README](README.md#contributing) 和 [TESTING.md](TESTING.md) 要求的适用检查后合并；未完成的功能留在工作分支。
4. 功能 PR 可以 squash 合并。日常无需等待发布日才集成完成的功能。

## 准备与发布

1. 发布负责人提交 `dev → develop` PR，确定本次版本的功能范围。
2. 该 PR 合并后冻结 `develop` 的功能范围；下一版功能继续进入 `dev`，本次发布期间不再把整个 `dev` 合入 `develop`。
3. 验收修复从 `develop` 创建短期分支，通过 PR 合回 `develop`。更新版本号和 `RELEASE_NOTES.md`，完成 `TESTING.md` 中的发布检查及人工验收。
4. 提交并审阅 `develop → main` 发布 PR，通过后合并。在对应的 `main` 提交上创建与包版本一致的 `vX.Y.Z` 标签并推送，触发现有 macOS 打包和发布工作流。合并分支本身不会发布。
5. 通过 PR 将发布后的 `main` 同步回 `develop`，再将 `develop` 同步回 `dev`，保留验收修复和版本信息；以后接入 fragments 时也必须同步已消费片段的删除。

长期分支之间使用普通 merge commit，保留共同提交历史，不使用 squash 或 rebase 合并。同步发生冲突时在临时分支解决，通过 PR 合回目标分支，不重置或强推长期分支。

紧急修复合入 `main` 后按相同发布检查创建新的修复版本标签，并通过上述同步路径回到 `develop` 和 `dev`；不得只修正式版而遗漏后续开发分支。

## 当前执行边界

- `dev` 和 `develop` 初始基线为 `main` 的 `6daf3c5`，随后共同包含本约定。已有 `v0.7.3`、`v0.7.4` 及后续功能分支中未进入该基线的改动，需要审阅后集成；分支创建不会自动合入这些改动。
- 本文是协作约定。GitHub 分支保护、PR 自动检查、fragments 校验和 release notes 自动生成尚未随本次分支建立而配置，不应宣称已自动执行。
- 当前发布工作流仍以版本标签或显式手动启动为入口；发布前由负责人核对版本号、发布说明和测试结果。
