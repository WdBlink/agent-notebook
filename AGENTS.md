Project context & docs live in ./ctx - managed by the ctx skills; apply their consistency rules when editing ctx docs.

Read [CONTRIBUTING.md](CONTRIBUTING.md) before branch, integration, or release work. Feature branches start from and return to `dev`; `develop` is the release stabilization branch; `main` is the stable release baseline. Use PRs for shared-branch changes and merge commits between long-lived branches. Preserve existing work and run the applicable checks in `TESTING.md`.
